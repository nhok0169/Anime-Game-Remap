name: Integration Tester

on: 
  pull_request:
  push:
  schedule:
    - cron: "*/5 * * * *"

jobs:
  run-unit-tests:
    name: 'Integration Tests'
    uses: './.github/workflows/integration-test-workflow.yml'
