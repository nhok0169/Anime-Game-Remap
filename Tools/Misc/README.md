# Misc

Scripts the `AI Agent Help/` guides refer to that were written next to the mods they operate on,
on the maintainer's machine (`E:\Computer\Games\Wuthering Waves Mods\Importer\GIMI\...`), copied
here so an agent on another machine can read and run them. **The copies under `Importer/GIMI/`
are the live ones the maintainer runs; when the two differ, the live one is newer.** Everything
here runs on Windows or Linux / WSL (a Windows-form path argument is translated to `/mnt/<drive>/...`
on Linux), and finds the API through `AG_REMAP_REPO` or the repo's own layout.

| Folder | What | Guide |
| --- | --- | --- |
| `Prototypes/overrideScript.py` | the **config route** prototype (`GIMICharParserConfig` / `GIMICharFixerConfig` handed to the shipped factories); `--ab` proves it byte-identical to the compiled fix | [Creating Remaps](../../AI%20Agent%20Help/CreatingRemaps/CLAUDE.md) |
| `Prototypes/overrideScript2.py` | the **hand-built route** prototype (a `GIMIParser` / `GIMIFixer` from the individual edits) | same |
| `Docs/genApiExamples.py` | **regenerates `Docs/src/apiExamples.rst`'s Input / Code / Result dropdowns** from the Integration Tester's `APIDocsTests` inputs and goldens; `--write` to write, idempotent over an up-to-date page | Documentation's "`apiExamples.rst` is GENERATED" |
| `Diagnostics/unitTestIds.py <out> [--api <copy>] [--only ...]` | runs the Unit Tester and writes every failure / error ID to a file to `diff`; `--api` tests another copy of the API (an OLD build) without touching the shared module, and imports it before the test modules' own `sys.path.insert(1, ...)` can outrank it | Overview's habit 50 |
| `Docs/pyiSplice.py <generated core.pyi> [--show / --apply Class ...]` | lists the classes a freshly generated stub changes, diffs them, and splices only the named ones into the tracked `core.pyi` | same; Building's stubgen note |
| `Docs/doxygenSplice.py --run <scratch>` then `<scratch>/xml [--show / --apply NAME ...]` | the Doxygen counterpart: runs Doxygen into a scratch folder (never `core/xml`) and prints the header warnings; then lists the compounds that differ from the tracked `core/xml` and splices only the named ones -- their `.xml` files AND their `<compound>` blocks in the TRACKED `index.xml`, so the index never names a file that is not there. Name a class and its header separately (`AGRemapCore::GraphInherit GraphInherit.h`). Proved against a copy of `core/xml` holding the old and the missing state | Overview's habit 41; Documentation |
| `Diagnostics/goldenChanges.py` | what each Integration Tester golden PRODUCED -- files added / removed / changed relative to its inputs, per test; an empty entry is a vacuous test | Testing's "Integration Tester" |
| `Linux/integrationTest.sh` | runs the Integration Tester on Linux/WSL (`runSuite` / `produceOutputs` [tests...]) with the venv and `~/itlib`; LF -- run a CRLF checkout of it through `tr -d "\r"` | same |
| `Prototypes/kiraraAltFix.py` | the **alternative Kirara -> KiraraBoots fix** (Kirara's body also drawn as KiraraBoots' head, diffuse darkened, KiraraBoots' body hidden) as a `GIMICharFixerConfig` override -- the updated copy of the maintainer's `Importer/GIMI/Kirara4/.../mainUpdated.py`, whose `main.py` beside it is the original pure-Python version. Same fix as the Integration Tester's `overrideFix/iniPath_ImplOverride.py` and the docs' "Override the Default Remap for a Character"; `--wsl` from Windows | Testing's "Integration Tester" |
| `Prototypes/overrideVgRemap.py` | a vertex-group remap at runtime: `--dump` prints a pair's `unmapped source groups`, `--ab` diffs the `RemapBlend.buf` bytes with and without a change | [Vertex Group Remaps](../../AI%20Agent%20Help/VGRemaps/CLAUDE.md) |
| `Prototypes/yelanTranquilFix.py` | **the Yelan -> YelanTranquil fix, whole**, through the API's parser / fixer / `RemapService`: a runtime `ModType`, one fixer per target component, the buffers as one resource group (`ResGroupCollect` + `BufReplace` + `VGSplitGroupResource`), the textures band-mapped and written with mip chains. Runs from WSL (`--wsl` from Windows). The thing the compiled fixer (`makeGIMIComponentFixer`, 2026-09-13) was transcribed from and is A/B'd against | VGRemaps recipe step 8; Creating Remaps' "Yelan is COMPILED now" |
| `Prototypes/yelanTranquilFixPerBuffer.py` | its earlier shape: one `ResRegCollect` + `fixFunc` per buffer (runs on an API without the split classes) | |
| `Prototypes/yelanTranquilFixStandalone.py` | its first shape, with no API at all | |
| `Prototypes/identityMod.py` | **the identity mod**: a character's own model as a GIMI mod out of `GI-Model-Importer-Assets/PlayerCharacterData/<Name>` -- the first mod to test any remap on, since every bone and every material band of the real skin is in it. Handles a skin of **several components** (`--textureFrom`, `--faceRegister`; YelanTranquil's is `Mods/YelanTranquilIdentity`) as well as the classic single-component shape | VGRemaps recipe step 8 |
| `Prototypes/wwmiIdentityMod.py` | **the identity mod for a Wuthering Waves character**: the game's own model as a WWMI mod out of `WWMI-Assets/PlayerCharacterData/<Name>` (`Metadata.json` + `Component N.fmt/.vb/.ib` + textures), in the BETA-2 `.ini` shape WWMI Tools 1.3.4 generates -- one merged mesh, the bones through each component's `vg_map`, the sparse shape keys rebuilt from the dumps' per-vertex `SHAPEKEY<k>` deltas (proved by reproducing Metadata's `checksum`), every texture overridden by hash. Needs only numpy. Built `WWMI/SanhuaIdentity` and `WWMI/SanhuaExorcistIdentity` on 2026-09-19. Since then it also takes an asset folder `wwmiExtractDump.py` pulled out of a frame dump, a character with **eight bone weights a vertex** (Augusta, Iuno, Chisa: the `.fmt` declares `R8_UINT` for an 8-byte element) and a merged skeleton **past 256 bones** through WWMI's **blend remap** -- `BlendRemapVertexVG.buf` (every vertex's full 16-bit ids), `BlendRemapForward` / `Reverse.buf` (512 uint16s per remapped component) and the `.ini` sections that run WWMI's `BlendRemapper` / `SkeletonRemapper`, as WWMI Tools 1.7.3's `build_blend_remap` writes them. Proved by simulating both shaders over the written files against the raw `.vb` bytes (every weighted slot of Augusta, Iuno, Galbrena, Changli and Chisa lands on its bone; a swapped reverse entry or a stripped `.ini` fails it), and byte-identical to the previous version on every character it could already build. Built `WWMI/ChisaIdentity` / `ChisaParfaitIdentity` from frame dumps (2026-09-19) -- **both correct in game**, the first in-game confirmation of the blend remap (Chisa's components 3-5) and of a dump-extracted asset folder | VGRemaps' "WuWa: Sanhua <-> SanhuaExorcist" |
| `Prototypes/wwmiExtractDump.py <FrameAnalysis> <out>` | **a WWMI-Assets-style asset folder out of a frame dump**, for a character WWMI-Assets does not have (Chisa, ChisaParfait): runs WWMI Tools' own "Extract Objects From Dump" (the Blender addon's `extract_frame_data`) outside Blender with `bpy` stubbed, skipping the `<call>.<n>-[ShaderRegex_...]` sub-call files RabbitFX adds, which the addon cannot parse. One folder per vb0 hash. Reproduces WWMI-Assets' GEOMETRY byte for byte (Sanhua, SanhuaExorcist); NOT its textures -- a dump holds each texture in whatever streaming state it was drawn in, usually 512 px, under a hash that changes as its mips load | Data/Mod Downloads/WuWa/README.md |
| `Prototypes/wwmiDownloadFolder.py <assets> <Data/Mod Downloads/WuWa/<Name>/<ver>> --name <Name> [--check]` | **a WuWa character's download folder from its asset folder**: the identity mod's buffers renamed `<Name><Buffer>.buf`, every texture as `<Name>Texture<hash>.dds`, the two manifests. `--check` builds into a temporary folder and compares with the shipped one -- it reproduces both Sanhua folders from WWMI-Assets exactly, and from the frame dumps every buffer and the Metadata | Data/Mod Downloads/WuWa/README.md |
| `Prototypes/sanhuaExorcistFix.py` | **the first WuWa remap, as a prototype, fixing IN PLACE through the API** (the Yelan / Bennett shape: a Python-built `GIMIParser` / `GIMIFixer` on `CppStrategyOverrides`, `RemapService` over the mod folder, the fix block in the mod's own `.ini`, `--undo` / `--hideOrig` / `--keepBackups`): each Sanhua slot section retargeted to its Exorcist slot by shader family (bodice, skirt and arm skin all onto the torso slot, as three sections on one draw), hashes and the shape-key checksum through `RegAssetRemap` and the new `ShapeKeyChecksums` table, the slot's four numbers through `RegNewVals` from `WWMIBuilder`'s tables, `SanhuaExorcistRemapBlend.buf` through `ResRegCollect` + a `RemapBlendReplace` whose `fixFunc` runs the API's `BlendFile` over the 8-byte WWMI layout, textures bound by register in a per-component command list gated on the target's main-pass pixel shaders (`[ShaderOverride]` `filter_index` tags), an invented solid `(255, 77, 0)` material mask for the arm skin -- the command lists, the hidden-slot section, the tags and the mask resource through the newly bound `GIMIFixer.appendedSections`. Since the first in-game report (2026-09-19): a LIST of tagged passes per slot (the bangs are drawn on two hair-shader passes), the iris on the eye slot, `--anchor ribbons|all` (the back ribbons and belt tassel, or every skirt chain, pinned to their root bone), `--vgRemap <json>` (any {source group: target group} table), `--shapeKeys hide|leave|retarget` (default `hide`: the mod's own shape-key sections commented out with the `HideOrig` marker, as the maintainer's working hand remap has them), `--plan rnd|rndArms` (that hand remap's slot plan: bodice through slot 4, arm skin dropped or through the torso slot) and `--hideTextureOverrides` -- the four bisect variants of 2026-09-19 are `WWMI/Sanhua2_A_rnd` .. `_D_shapeKeysLeft`, and `_E_zeroOffsets` is their answer: every remapped section binds `vb6`, the game's per-vertex shape-key offset stream that the torso / face / eye vertex shaders read by vertex id, to a zero buffer the fix writes (`<Target>RemapShapeKeyZero.buf`) -- which did not clear it; `_F_split` tries one remapped section per target draw per file (the default now, `--noSplit` for one file), the rest in `<stem>SanhuaExorcistRemapFix<n>.ini`. Test mods: `WWMI/SanhuaIdentityOnExorcist` (also in `Mods`), `...Anchored` (`--anchor ribbons`), `Sanhua2OnExorcist`, `Sanhua1OnExorcist` | VGRemaps' "WuWa: Sanhua <-> SanhuaExorcist" |
| `Diagnostics/wwmiTextureFix.py <mod> --assets <Data/Mod Downloads/WuWa/<Name>/<ver> or a WWMI-Assets folder> [--maps hash_maps.json] [--apply]` | **brings an older WuWa mod's texture overrides up to the current game version, and ships the textures it references but lacks** -- the two silent ways a WuWa mod's textures stop working. An old hash is resolved by the community maps, else by the mod's own file being pixel-identical (image correlation over 0.95, colour and alpha) to exactly one current texture, whatever its size or format (authors ship upscaled copies); a repainted texture is left alone on purpose. `Data/Mod Downloads/WuWa/Sanhua/SanhuaHashLineage.json` is the measured old -> current map for Sanhua, to pass as `--maps` beside the community tables. The remap prototype reuses its decode / correlation to place a mod texture that carries no hash at all (pixel identity with a game texture). A referenced file the mod does not carry is copied in from the assets under the referenced name (the material masks an author forgot go black otherwise). Report only without `--apply`; the original is kept as `DISABLED <name>_texfix_backup`. First run 2026-09-19 on the Witch Sanhua mod: 6 hashes, 2 missing masks | VGRemaps' "WuWa: Sanhua <-> SanhuaExorcist" |
| `Diagnostics/wwmiBoneTally.py <source WWMI mod> <target WWMI mod> --library Sanhua SanhuaExorcist` (or `--json`, `--sheet`) | **the WuWa form of `modTally.py --remap`**: per source vertex group, its vertices' centroid on the source mod, the target bone the remap sends it to, that bone's centroid on the target mod and the distance between them, then every chain of consecutive groups with the targets it lands on. Run it on the two identity mods. A chain landing 5-20 cm from where its vertices sit curls in game once it bends (Sanhua's back ribbons); fingers at 0.0 and skirt chains mapped in order are fine. `--library` needs `py -3`, `--sheet` needs openpyxl (`py -3.11`) | VGRemaps' "WuWa: Sanhua <-> SanhuaExorcist" |
| `Diagnostics/wwmiDrawTable.py` | **the per-draw texture slot table of a character in a WWMI frame dump** (`log.txt`): every draw of the character's vertex buffer, which component by index range, the vertex / pixel shaders, and what hash sits in every `ps-t` slot -- the WuWa form of "parse the mod's own sections into a per-slot table". It is how the shader families and slot roles above were read, and what to re-run when a game update moves them | VGRemaps' "WuWa: Sanhua <-> SanhuaExorcist" |
| `Diagnostics/modTally.py` | per object of a mod: geometry, vertex groups, lightmap band at its own vertices with the diffuse colour under each band; `--against` another mod, `--remap` against the shipped row, `--centroids` | Creating Remaps' "The Yelan lessons" |
| `Diagnostics/boneCentroids.py` | every vertex group's centroid / weight / dominated vertices of a character, from a frame analysis (`--hashes`), a dump folder or a mod folder -- for choosing an anchor for a part the target lacks | same |
| `YelanExperiments/` | the hand-experiment generators from the china-dress test mod (`NegIndexFilter.py`, `GraphCutFilter.py`, `MixedFilter.py` on top of `Tools/VGRemapFinder`'s `ComponentSplit.py`; `LiftBodyLightMap.py`, `EditVertexColour.py`, `MatchHairColour.py`), `YelanTranquil_hash.json` they read, and the maintainer's **hand-made reference pair** `YelanHandMade.ini` / `YelanCopyHandMade.ini` (the double-file shape done by hand, the A/B reference that found the vertex-limit and texture omissions) | VGRemaps "A character of SEVERAL components" |
| `Linux/linuxBuild.sh` | rebuild the core module under WSL from the native build tree and drop the `.so` into the package folder, printing before / after mtimes and the exit codes | [Building](../../AI%20Agent%20Help/Building/CLAUDE.md) |
| `Linux/buildTests.sh <Test> ...` | compile and run named `core/tests/*.cpp` standalone tests on Linux against the native build tree | [Testing](../../AI%20Agent%20Help/Testing/CLAUDE.md) |
| `Diagnostics/abWWMI.py <mod folder> [--proto <script>]` | **A/Bs a WuWa mod through the PROTOTYPE and through the COMPILED tables**: two scratch copies, both undone first, one fixed by `sanhuaExorcistFix.py` and one by `RemapService` alone, every file diffed (buffers by md5, `.ini` line by line). What proved `makeWWMIFixer` on four mods (2026-09-19) | Creating Remaps' "WUWA IS COMPILED" |
| `Diagnostics/wwmiTextureThumbs.py <download folder> --name <Char> --out <cpp>` | **generates a WWMI character's texture THUMBPRINT table** (a 16 x 16 grayscale box average per game texture, through the API's own `TextureFile`) as the C++ file a `WWMIFixerConfig::textureThumbprints` is filled from -- how the compiled fixer places a hash-less mod texture by its pixels without downloading anything | `IniFixData/Sanhua/SanhuaThumbprints.cpp` |
| `Diagnostics/runCompiled.py <mod folder>` | fix a folder through the COMPILED tables alone and print every stats bucket -- the compiled half of an A/B against a prototype (fix a scratch copy with each, diff the folders) | Creating Remaps' "Yelan is COMPILED now" |
| `Diagnostics/slotTextures.py <mod folder> ...` | **the table to read before reasoning about textures at all**: per draw slot (a `TextureOverride` with a `match_first_index`), whether the mod declares any `ps-t` -- and so whether that slot renders with the MOD's art or the GAME's -- and then, if the folder has been fixed, what each draw of each remapped section binds. Reports; does not decide | Creating Remaps' "A `TextureOverride` binds registers only for the draw its hash matches" |
| `Diagnostics/drawCoverage.py <fixed folder> ...` | every remapped section's draws with their `if` DEPTH, flagging `auto` stacked on an explicit range, and a part drawn only from inside a toggle. **Depth is the point** -- a check that `.strip()`s the line cannot see nesting, which is what let a fix ship with the eyes on one toggle | Creating Remaps' "A target object several components merge onto is not one draw" |
| `Diagnostics/fixCallPaths.py <fixed folder> ...` | does any path call `NNFix` / `ORFix` twice **over one set of bindings**? They are involutions, so the second undoes the first and the model renders flat green. Counts per path per BINDING GENERATION -- an exclusive `if` / `else if` chain is one call, and a `ps-t` rebinding resets the count | Creating Remaps' "The fix libraries are involutions" |
| `Diagnostics/check_dangling.py <fixed folder>` | does every `filename =` a fixed `.ini` names exist on disk? A section diff compares `.ini` TEXT, so a resource the run never produced -- or produced and deleted -- diffs perfectly and is a missing mesh in game. Skips `DISABLED` files | Creating Remaps' "The A/B diff cannot see a missing file" |
| `Diagnostics/check_sections.py <fixed folder>` | the companion shape: does every `<register> = Resource...` name a section the FOLDER defines? A reference with no section has no filename for `check_dangling` to follow, so it passes there -- CherryHuTao's body drew with no light map that way. Pools defined names per folder, since a merge splits its resources across files | same |
| `Diagnostics/cmp_binaries.py <old tree> <new tree>` | compare every generated `.buf` / `.dds` / `.ib` of two fixed trees by content hash, enumerating BOTH sides so a file only one produced shows up. Matches the generated-name markers anywhere in the name -- an edited texture's name ends in a hash, and an `endswith` test once skipped every one of them | Creating Remaps' "A texture edit's output file is named with two hashes" |
| `Diagnostics/fixUndoCycle.py <original mod> <scratch>` | fix a scratch copy, UNDO it, and require every `.ini` back to the original section by section -- no leftover or lost sections, no `remap` text -- after reporting whether each `...Hide` section landed inside the remap block. An undo only takes what a fix wrote inside its block, so a template writing outside it passes every other check and still hides a component of the unmodded skin after undo | Creating Remaps' "Undo is only as complete as what the fix wrote INSIDE its block" |
| `Diagnostics/abIni.py <folder A> <folder B>` | A/B two fixed folders' `.ini` files SEMANTICALLY -- every resource reduced to the md5 of the file it names, `if`/`endif` dropped, key order kept. **Byte-identical buffers say nothing about the `.ini`**: one run had perfect buffers and `HashNotFound` in every section | Creating Remaps' "Byte-identical buffers say nothing about the `.ini`" |
| `Diagnostics/checkWorkflowWiring.py` | checks `.github/workflows`' reusable-workflow wiring the way GitHub only finds out at run time -- undeclared or missing inputs, `needs.X.outputs.Y` naming nothing, a tester called without the build artifact (it fails at import) -- and prints the **status-check names** each workflow reports, which branch protection matches on. Exit 1 on a problem; proved to fail on a copy with the artifact removed | [CI](../../AI%20Agent%20Help/CI/CLAUDE.md) |
| `Diagnostics/uvIsland.py <mod folder> <Texcoord.buf> <tex.dds> <out.png>` | crop the UV island an object occupies out of a texture and save it as a PNG, vertices marked, so it can be **looked at**. Refuses to guess the texcoord stride when more than one layout gives UVs in [0, 1] (they routinely do) -- pass `--stride` | Creating Remaps' "When the symptom is on a texture, crop the UV island and look at it" |
| `Diagnostics/purpleSlot.py <mod folder> --component <C>` | rebind ONE register of one target component's remapped sections to a flat magenta texture it writes itself (an uncompressed DDS -- no Compressonator, no Pillow DDS writer), backing up each `.ini`; `--off` restores them. **The answer does not depend on being right about anything**: the slot turns magenta and that section is what draws it, or it does not and the section is not | Overview's "When you cannot tell what a draw is using" |

Nothing tests this folder; run a script before you change it (see
[Tools](../../AI%20Agent%20Help/Tools/CLAUDE.md)). The `YelanExperiments/` scripts carry absolute
paths from the maintainer's machine near their top -- they are reference, not tooling.
